#version 100
precision mediump float;
precision highp int;

struct type_CustomBuffer
{
    highp vec4 _MainTex_TexelSize;
    highp float _BlurRadius;
};

uniform type_CustomBuffer CustomBuffer;

uniform highp sampler2D _MainTex;

varying highp vec2 varying_TEXCOORD0;

void main()
{
    highp vec2 _41 = CustomBuffer._MainTex_TexelSize.xy * CustomBuffer._BlurRadius;
    gl_FragData[0] = vec4((((((texture2D(_MainTex, varying_TEXCOORD0) + texture2D(_MainTex, varying_TEXCOORD0 + _41)) + texture2D(_MainTex, varying_TEXCOORD0 + (_41 * vec2(-1.0, 1.0)))) + texture2D(_MainTex, varying_TEXCOORD0 + (_41 * vec2(-1.0)))) + texture2D(_MainTex, varying_TEXCOORD0 + (_41 * vec2(1.0, -1.0)))) * 0.20000000298023223876953125).xyz, 1.0);
}

