#version 100
precision mediump float;
precision highp int;

uniform highp sampler2D _CameraDepthTexture;

varying highp vec2 varying_TEXCOORD0;

void main()
{
    gl_FragData[0] = vec4(texture2D(_CameraDepthTexture, varying_TEXCOORD0).xyz, 1.0);
}

